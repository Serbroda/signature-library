{
  "name": "signature-pad",
  "version": "0.0.1",
  "description": "Signature Pad",
  "main": "index.js",
  "scripts": {
    "start": "npm run watch",
    "watch:tsc": "tsc -w",
    "watch:gulp": "gulp watch",
    "watch": "concurrently --kill-others \"npm run watch:tsc\" \"npm run watch:gulp\"",
    "build:tsc": "tsc",
    "build:gulp": "gulp bundle && gulp minify",
    "build": "npm run build:tsc && npm run build:gulp"
  },
  "keywords": [
    "signature",
    "pad"
  ],
  "author": "Danny Rottstegge",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^3.5.1",
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.1",
    "gulp-copy": "^1.1.0",
    "gulp-minify": "^2.1.0",
    "gulp-watch": "^5.0.0",
    "tsc": "^1.20150623.0",
    "typescript": "^2.8.3"
  }
}
